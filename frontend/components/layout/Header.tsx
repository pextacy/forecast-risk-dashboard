import React, { useState } from 'react';\nimport { motion } from 'framer-motion';\nimport { BellIcon, Cog6ToothIcon, UserCircleIcon } from '@heroicons/react/24/outline';\nimport { useSystemStatus } from '../../hooks/useSystemStatus';\n\nexport default function Header() {\n  const [showNotifications, setShowNotifications] = useState(false);\n  const { data: systemStatus } = useSystemStatus();\n\n  return (\n    <header className=\"fixed top-0 left-0 right-0 z-50 bg-white/80 backdrop-blur-md border-b border-gray-200\">\n      <div className=\"px-4 sm:px-6 lg:px-8\">\n        <div className=\"flex items-center justify-between h-16\">\n          {/* Logo and Title */}\n          <div className=\"flex items-center\">\n            <motion.div\n              initial={{ scale: 0 }}\n              animate={{ scale: 1 }}\n              transition={{ type: \"spring\", stiffness: 260, damping: 20 }}\n              className=\"flex-shrink-0 flex items-center\"\n            >\n              <div className=\"text-2xl font-bold bg-gradient-to-r from-blue-600 to-indigo-600 bg-clip-text text-transparent\">\n                💼 Treasury AI\n              </div>\n            </motion.div>\n            \n            {/* System Status Indicator */}\n            <div className=\"ml-6 hidden sm:flex items-center space-x-2\">\n              <div className={`w-2 h-2 rounded-full ${\n                systemStatus?.database === 'healthy' && systemStatus?.services?.ingestion === 'healthy'\n                  ? 'bg-green-400 animate-pulse'\n                  : 'bg-red-400'\n              }`} />\n              <span className=\"text-xs text-gray-500\">\n                {systemStatus?.database === 'healthy' ? 'All Systems Operational' : 'System Issues Detected'}\n              </span>\n            </div>\n          </div>\n\n          {/* Right Side Actions */}\n          <div className=\"flex items-center space-x-4\">\n            {/* Data Freshness */}\n            {systemStatus?.data_freshness && (\n              <div className=\"hidden md:flex items-center space-x-2 text-xs text-gray-500\">\n                <span>📊</span>\n                <span>\n                  Data: {systemStatus.data_freshness.status === 'fresh' ? 'Fresh' : 'Stale'}\n                  {systemStatus.data_freshness.hours_ago && (\n                    ` (${systemStatus.data_freshness.hours_ago}h ago)`\n                  )}\n                </span>\n              </div>\n            )}\n\n            {/* Notifications */}\n            <div className=\"relative\">\n              <button\n                onClick={() => setShowNotifications(!showNotifications)}\n                className=\"p-2 text-gray-400 hover:text-gray-500 hover:bg-gray-100 rounded-full transition-colors\"\n              >\n                <BellIcon className=\"h-5 w-5\" />\n                <span className=\"absolute top-1 right-1 block h-2 w-2 rounded-full bg-red-400\"></span>\n              </button>\n              \n              {showNotifications && (\n                <motion.div\n                  initial={{ opacity: 0, scale: 0.95 }}\n                  animate={{ opacity: 1, scale: 1 }}\n                  className=\"absolute right-0 mt-2 w-80 bg-white rounded-lg shadow-lg border border-gray-200 py-2 z-50\"\n                >\n                  <div className=\"px-4 py-2 border-b border-gray-100\">\n                    <h3 className=\"text-sm font-medium text-gray-900\">Notifications</h3>\n                  </div>\n                  <div className=\"max-h-64 overflow-y-auto\">\n                    <div className=\"px-4 py-3 text-sm text-gray-600\">\n                      <div className=\"flex items-center space-x-2\">\n                        <span className=\"text-green-500\">✅</span>\n                        <span>Portfolio risk analysis completed</span>\n                      </div>\n                    </div>\n                    <div className=\"px-4 py-3 text-sm text-gray-600\">\n                      <div className=\"flex items-center space-x-2\">\n                        <span className=\"text-blue-500\">🔮</span>\n                        <span>New Bitcoin forecast available</span>\n                      </div>\n                    </div>\n                    <div className=\"px-4 py-3 text-sm text-gray-600\">\n                      <div className=\"flex items-center space-x-2\">\n                        <span className=\"text-amber-500\">⚠️</span>\n                        <span>High volatility detected in portfolio</span>\n                      </div>\n                    </div>\n                  </div>\n                </motion.div>\n              )}\n            </div>\n\n            {/* Settings */}\n            <button className=\"p-2 text-gray-400 hover:text-gray-500 hover:bg-gray-100 rounded-full transition-colors\">\n              <Cog6ToothIcon className=\"h-5 w-5\" />\n            </button>\n\n            {/* User Menu */}\n            <button className=\"p-2 text-gray-400 hover:text-gray-500 hover:bg-gray-100 rounded-full transition-colors\">\n              <UserCircleIcon className=\"h-5 w-5\" />\n            </button>\n          </div>\n        </div>\n      </div>\n    </header>\n  );\n}"